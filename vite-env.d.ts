// Since this is a TypeScript declaration file (*.d.ts), we don't write tests for it
// Declaration files only contain type definitions and are not testable units
// They are used by TypeScript for type checking but don't contain runtime code
